version: '3'
services:
  service:
    restart: unless-stopped
    build: ./upload/
    ports:
      - "127.0.0.1:3008:8080"
    volumes:
      - $UPLOAD_DIR:/go/src/app/upload
    #    logging:
    #      driver: none
    environment:
      CHECK_URL: "https://www.rockauto.com/"