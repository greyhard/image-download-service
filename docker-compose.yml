version: '3'
services:
  service:
    restart: unless-stopped
    build: ./upload/
    ports:
      - "127.0.0.1:3008:8080"
    volumes:
      - ./images/:/upload/
    logging:
      driver: none
    environment:
      CHECK_URL: "https://www.rockauto.com/"